package com.maximo.douglas.githubconsumer.components.gitpullrequestcard.context

import org.junit.Before
import org.junit.Test

class GitPullRequestCardUserImageTest : GitPullRequestCardTest() {

    @Before
    fun `setup scenario`() {
        `when pull request card has all data`()
    }

    @Test
    fun `test if user image is visible`() {
        `assert that user image is visible`()
    }

}