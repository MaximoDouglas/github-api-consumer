package com.maximo.douglas.githubconsumer.components.gitrepositorycard.context

import org.junit.Before
import org.junit.Test

class GitRepositoryCardRepositoryNameTest : GitRepositoryCardTest() {

    @Before
    fun `setup scenario`() {
        `when repository card has all data`()
    }

    @Test
    fun `test if repository name is visible`() {
        `assert that repository name is visible`()
    }

    @Test
    fun `test if repository name is set correctly`() {
        `assert that repository name is set correctly`()
    }

}