package com.maximo.douglas.githubconsumer.components.gitpullrequestcard.context

import org.junit.Before
import org.junit.Test

class GitPullRequestCardTitleTest : GitPullRequestCardTest() {

    @Before
    fun `setup scenario`() {
        `when pull request card has all data`()
    }

    @Test
    fun `test if pull request title is visible`() {
        `assert that pull request title is visible`()
    }

    @Test
    fun `test if pull request title is set correctly`() {
        `assert that pull request title is set correctly`()
    }

}