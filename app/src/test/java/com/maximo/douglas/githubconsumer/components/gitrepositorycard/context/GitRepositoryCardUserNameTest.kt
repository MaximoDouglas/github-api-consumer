package com.maximo.douglas.githubconsumer.components.gitrepositorycard.context

import com.maximo.douglas.githubconsumer.components.gitrepositorycard.context.GitRepositoryCardTest
import org.junit.Before
import org.junit.Test

class GitRepositoryCardUserNameTest : GitRepositoryCardTest() {

    @Before
    fun `setup scenario`() {
        `when repository card has all data`()
    }

    @Test
    fun `test if user name is visible`() {
        `assert that user name is visible`()
    }

    @Test
    fun `test if user name is set correctly`() {
        `assert that user name is set correctly`()
    }

}
