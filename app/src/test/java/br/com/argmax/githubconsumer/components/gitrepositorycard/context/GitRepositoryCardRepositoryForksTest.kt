package br.com.argmax.githubconsumer.components.gitrepositorycard.context

import org.junit.Before
import org.junit.Test

class GitRepositoryCardRepositoryForksTest : GitRepositoryCardTest() {

    @Before
    fun `setup scenario`() {
        `when repository card has all data`()
    }

    @Test
    fun `test if repository forks quantity label is visible`() {
        `assert that repository forks quantity label is visible`()
    }

    @Test
    fun `test if repository forks quantity label is set correctly`() {
        `assert that repository forks quantity label is set correctly`()
    }

}
