package br.com.argmax.githubconsumer.components.gitpullrequestcard.context

import org.junit.Before
import org.junit.Test

class GitPullRequestCardWithBodyTest : GitPullRequestCardTest() {

    @Before
    fun `setup scenario`() {
        `when pull request card has all data`()
    }

    @Test
    fun `test if pull request body is visible`() {
        `assert that pull request body is visible`()
    }

    @Test
    fun `test if pull request body is set correctly`() {
        `assert that pull request body is set correctly`()
    }

}