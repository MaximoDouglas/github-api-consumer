package br.com.argmax.githubconsumer.components.gitrepositorycard.context

import org.junit.Before
import org.junit.Test

class GitRepositoryCardRepositoryDescriptionTest : GitRepositoryCardTest() {

    @Before
    fun `setup scenario`() {
        `when repository card has all data`()
    }

    @Test
    fun `test if repository description is visible`() {
        `assert that repository description is visible`()
    }

    @Test
    fun `test if repository description is set correctly`() {
        `assert that repository description is set correctly`()
    }

}