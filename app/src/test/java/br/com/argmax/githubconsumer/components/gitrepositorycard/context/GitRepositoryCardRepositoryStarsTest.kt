package br.com.argmax.githubconsumer.components.gitrepositorycard.context

import org.junit.Before
import org.junit.Test

class GitRepositoryCardRepositoryStarsTest : GitRepositoryCardTest() {

    @Before
    fun `setup scenario`() {
        `when repository card has all data`()
    }

    @Test
    fun `test if repository stars quantity label is visible`() {
        `assert that repository stars quantity label is visible`()
    }

    @Test
    fun `test if repository stars quantity label is set correctly`() {
        `assert that repository stars quantity label is set correctly`()
    }

}